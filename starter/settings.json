{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "$comment": "senior-mode starter: two mindset hooks. Lives at ~/.claude/settings.json so it applies in every repo. If you already have a settings.json, merge the hooks block in (or run install.sh, which does it for you). Both hooks fail open: a broken hook never blocks work. Remove the Stop block if the end-of-turn self-check feels like nagging.",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$HOME/.claude/hooks/senior-check-before.sh\"",
            "timeout": 10
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$HOME/.claude/hooks/senior-check-after.sh\"",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
